Doesn't use WMI. Support Toorbo Boost.
Memory usage, number of processes and uptime is discret values. CPU load and speed average between two updates (the interval specified in the settings). Note: the first time widget shows the average CPU load and speed since the start of the system (before the update).
